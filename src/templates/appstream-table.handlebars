<div class="panel filters-panel" data-role="selection">
    <p><strong>Filters</strong></p>
    {{#each filters}}
        <label><input type="checkbox" name="toggle-{{@key}}"{{#if display}} checked="checked"{{/if}}> {{label}}</label>
    {{/each}}
</div>
<div data-role="tablelist">
 </div>